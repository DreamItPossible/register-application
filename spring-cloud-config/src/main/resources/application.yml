server:
  port: 7000

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DreamItPossible/springcloud-config.git
          username: DreamItPossible
          password: chentao19871001

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/,http://localhost:8889/eureka/

management:
  security:
    enabled: false