server:
  port: 5000

spring:
  application:
    name: api-geteway

zuul:
  routes:
    hello-service:
      path: /hello-service/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/,http://localhost:8889/eureka/